apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: auth-db-secrets
  namespace: microservices
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: auth-db-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_HOST
      remoteRef:
        key: microservices/auth-db
        property: host
    - secretKey: DB_PORT
      remoteRef:
        key: microservices/auth-db
        property: port
    - secretKey: DB_NAME
      remoteRef:
        key: microservices/auth-db
        property: name
    - secretKey: DB_USER
      remoteRef:
        key: microservices/auth-db
        property: user
    - secretKey: DB_PASSWORD
      remoteRef:
        key: microservices/auth-db
        property: password
