apiVersion: v1
kind: Service
metadata:
  name: authentication-service
  namespace: microservices
spec:
  selector:
    app: authentication-service
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
  type: ClusterIP
